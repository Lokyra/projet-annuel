  /*
    $verificationToken = bin2hex(random_bytes(32));
    $req = $bdd->prepare("UPDATE users SET verification_token = :token WHERE email = :email");
    $req->execute([':email' => $_POST['email'], ':token' => $verificationToken]);
*/
    if (sendVerificationEmail($_POST['email'], $verificationToken)) {
        echo '<script>alert("Email sent successfully.");</script>';
    } else {
        echo '<script>alert("Failed to sent mail.");</script>';
    }